@page
@model BooktrackingApplication.Pages.CategoryTypes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CategoryType[0].Type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CategoryType[0].Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CategoryType) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.CategoryTypeID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.CategoryTypeID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.CategoryTypeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
